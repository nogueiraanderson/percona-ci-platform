apiVersion: v2
name: kube-prometheus-stack
version: 0.1.0
description: Prometheus operator + Prometheus + Alertmanager + Grafana + node-exporter + KSM.
type: application
dependencies:
  - name: kube-prometheus-stack
    version: 84.4.0
    repository: https://prometheus-community.github.io/helm-charts
